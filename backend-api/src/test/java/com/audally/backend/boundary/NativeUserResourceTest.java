package com.audally.backend.boundary;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeUserResourceTest extends UserResourceTest {

    // Execute the same tests but in native mode.
}